# trs-interop

Cross-implementation interoperability evidence package.

Tracks whether independent TRS implementations:

- pass conformance vectors,
- sync with reference,
- reconstruct replay-compatible state,
- preserve conflict and authorization visibility.

Primary artifact: `matrix.md`.
