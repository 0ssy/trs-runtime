# Ambiguity Report — impl-0001-independent-python

This report records all material ambiguities found while implementing against the supplied spec artifacts and executable vectors.

| ID | Ambiguity | Implemented interpretation | Proposed clarification |
|---|---|---|---|
| A-001 | The contributor instructions name `DesignRecord.pdf` or `.md`, but the repository contains `Design_Record.pdf`. | Treat `Design_Record.pdf` as the Design Record source; retain the naming discrepancy in the report. | Standardize the filename in the contributor instructions and baseline hash list, or explicitly allow both spellings. |
| A-002 | Section 6.5 requires an explicit genesis declaration, but supplied genesis vectors have `causes: []` and no explicit genesis field. | Empty causal references are accepted as the fixture’s genesis representation; signature presence remains required. | Add a normative `genesis: true` field or define the canonical zero-cause representation in the vector schema. |
| A-003 | Section 5.1 requires a cryptographic signature, while vectors provide opaque strings such as `sig:g1` and no public-key or algorithm fields. | Validate that a signature value is present and non-empty; do not claim cryptographic verification from fixture strings. | Specify signature algorithm, key material, encoding, and verification failure behavior. |
| A-004 | Authorization Traceability describes capability grants, but the vectors use authorization references to records without a fully specified capability payload grammar. | Validate reference existence and traverse authorization/cause links for evidence; do not infer domain capability meaning. | Define the minimum machine-checkable authorization payload and the exact grant-to-action matching algorithm. |
| A-005 | Observation Origin is normative, but supplied observations do not include an `origin` field. | Do not reject the supplied vectors for absent origin; validate origin when present and record the fixture gap. | Add `origin` to every observation vector or define a backward-compatible default. |
| A-006 | Schema Reference requires domain payloads to reference exactly one Schema payload, but the envelope has a schema string and no separate schema-record reference field. | Treat the envelope schema declaration as structural typing; do not impose a domain schema graph rule not expressible by the fixtures. | Define whether `schema` is a type tag, a reference ID, or both, and provide schema-reference vectors. |
| A-007 | “Mutually exclusive” assertions are not formally defined for opaque payloads. | Use the amendment’s operational subject-scoped rule: same causal parent, same primitive type and subject, differing payloads are surfaced as a conflict. | Specify an explicit conflict relation or state that the subject/payload inequality rule is normative. |
| A-008 | The supplied gate command refers to `research/interop/run_gate1_independent_impls_status.py`, which is absent from the archive. | Gate verification is implemented locally below using the required four metadata statuses and reports the missing upstream script as a repository gap. | Add the gate script to the repository or provide its canonical location and invocation for POSIX environments. |

No unresolved ambiguity was silently chosen without being recorded above. The implementation deliberately avoids payload interpretation beyond the structural checks required by the supplied vectors.
