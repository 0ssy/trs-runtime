# trs-sdk-cpp

C++ SDK scaffold for `trs-node` with parity methods:

- `health()`
- `submit(recordJson)`
- `query(exprJson)`
- `sync(recordsJson)`
- `replay()`

Current implementation is transport-focused and returns JSON strings to keep dependencies minimal.

