# Runtime

`trs-runtime` is the frozen kernel.

Responsibilities:

- record model
- append-only storage interface and backends
- verifier rule execution
- replay engine
- graph/query/sync primitives

Non-responsibilities:

- product workflows
- policy preferences
- transport APIs

