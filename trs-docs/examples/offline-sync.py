from __future__ import annotations

# Demonstrates independent progress during partition and post-reconnect sync.

print("Offline sync example: exchange records, append, verify, replay convergence.")

