from __future__ import annotations

# Demonstrates semantic interoperability through schema mapping, not runtime changes.

print("Schema mapping example: bags <-> kilograms using transformation layer.")

